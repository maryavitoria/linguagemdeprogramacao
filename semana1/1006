#include <stdio.h>

int main() {
  double A, B, C, MEDIA;
  double p1=2;
  double p2=3;
  double p3=5;
  
  scanf("%lf", &A);
  scanf("%lf", &B);
  scanf("%lf", &C);

  MEDIA= (A*p1+B*p2+C*p3)/10;

  printf("MEDIA = %.1lf\n", MEDIA);

}
