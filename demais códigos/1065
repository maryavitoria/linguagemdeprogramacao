#include <stdio.h>

int main() {
    int pares= 0;
    int x;
    int i;  
    
    for(i=0; i < 5; i++){
            
            scanf("%d", &x);
            
            if(x%2 == 0){
                pares++;  
            }
    }

    printf("%d valores pares\n", pares);

   return (0);
}
